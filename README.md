# Neural_Network
This project is an interactive neural network simulator built with Streamlit, Having the input values with it's weights and a weighted sum to sum up its values and then compare with the thershold value for the result.
